# Supabase configuration for bypass proxy
VITE_SUPABASE_URL=

# SMTP Email Configuration fallback (Nodemailer)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=

# MSG91 Campaign API Configuration (Primary 2FA delivery)
MSG91_AUTHKEY=
